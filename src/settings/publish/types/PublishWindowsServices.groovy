package settings.publish.types

import settings.publish.PublishItem

class PublishWindowsServices extends PublishCompiled {
    PublishWindowsServices(def steps, PublishItem publishItem) {
        super(steps, publishItem)
    }
}
